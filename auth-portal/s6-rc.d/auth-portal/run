#!/bin/sh
# Auth-Portal Go backend service

# Set default config path if not provided
export AUTH_PORTAL_CONFIG="${AUTH_PORTAL_CONFIG:-/app/configs/auth-portal.yaml}"

# Change to app directory
cd /app

# Execute auth-portal
exec /app/auth-portal
